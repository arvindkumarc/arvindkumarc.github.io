name = "rss-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Deploy to your workers.dev subdomain (change if you want a custom route)
# routes = [
#   { pattern = "rss-proxy.arvindc.in/*", zone_name = "arvindc.in" }
# ]

# KV namespace for accumulating the aggregated feed blob.
# Create with: wrangler kv namespace create FEEDS_KV
# then paste the returned id below.
[[kv_namespaces]]
binding = "FEEDS_KV"
id = "ec997c749d754cafbfc42b520376221a"
